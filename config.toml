# date_aware_plugin - 自动生成的配置文件
# 让 Bot 能够感知并展示昨天、今天、明天的日期信息，包括星期几和节假日

# 插件配置
[plugin]

# 是否启用插件
enabled = true

# 配置文件版本
config_version = "1.0.0"


# 日期功能配置
[date]

# 是否启用 LLM 扩展日期信息（不建议启用）
enable_llm_expand = false

# 使用的模型名称
llm_model = "replyer"

# 是否启用自动注入 Action
enable_action = true


